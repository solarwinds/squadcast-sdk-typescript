/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsRolesCreate } from "../funcs/teamsRolesCreate.js";
import { teamsRolesRemove } from "../funcs/teamsRolesRemove.js";
import { teamsRolesUpdate } from "../funcs/teamsRolesUpdate.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Roles extends ClientSDK {
  /**
   * Create Team Role
   *
   * @remarks
   * Add team's role to the team with given ability if not exists. Returns the role object in response.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async create(
    request: operations.TeamsCreateTeamRoleRequest,
    options?: RequestOptions,
  ): Promise<operations.TeamsCreateTeamRoleResponse> {
    return unwrapAsync(teamsRolesCreate(
      this,
      request,
      options,
    ));
  }

  /**
   * Remove Team Role
   *
   * @remarks
   * Remove team's role from the team. Upon success, the team's role will be removed from the team.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async remove(
    request: operations.TeamsRemoveTeamRoleRequest,
    options?: RequestOptions,
  ): Promise<Uint8Array> {
    return unwrapAsync(teamsRolesRemove(
      this,
      request,
      options,
    ));
  }

  /**
   * Update Team Role
   *
   * @remarks
   * Update team's role abilities and name.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async update(
    request: operations.TeamsUpdateTeamRoleRequest,
    options?: RequestOptions,
  ): Promise<operations.TeamsUpdateTeamRoleResponse> {
    return unwrapAsync(teamsRolesUpdate(
      this,
      request,
      options,
    ));
  }
}
