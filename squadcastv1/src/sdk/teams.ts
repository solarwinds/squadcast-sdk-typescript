/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsCreate } from "../funcs/teamsCreate.js";
import { teamsGetById } from "../funcs/teamsGetById.js";
import { teamsGetRoles } from "../funcs/teamsGetRoles.js";
import { teamsList } from "../funcs/teamsList.js";
import { teamsListMembers } from "../funcs/teamsListMembers.js";
import { teamsRemove } from "../funcs/teamsRemove.js";
import { teamsUpdate } from "../funcs/teamsUpdate.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { Roles } from "./roles.js";
import { TeamsMembers } from "./teamsmembers.js";

export class Teams extends ClientSDK {
  private _members?: TeamsMembers;
  get members(): TeamsMembers {
    return (this._members ??= new TeamsMembers(this._options));
  }

  private _roles?: Roles;
  get roles(): Roles {
    return (this._roles ??= new Roles(this._options));
  }

  /**
   * Get All Teams
   *
   * @remarks
   * Returns all the teams of the organization.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `read` scope.
   */
  async list(
    options?: RequestOptions,
  ): Promise<operations.TeamsGetAllTeamsResponse> {
    return unwrapAsync(teamsList(
      this,
      options,
    ));
  }

  /**
   * Create Team
   *
   * @remarks
   * Add team to the organization. Returns the team object in response.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async create(
    request: models.V3TeamsCreateTeamRequest,
    options?: RequestOptions,
  ): Promise<operations.TeamsCreateTeamResponse> {
    return unwrapAsync(teamsCreate(
      this,
      request,
      options,
    ));
  }

  /**
   * Get Team By ID
   *
   * @remarks
   * Returns a team details of the given `teamID` in the request param.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `read` scope.
   */
  async getById(
    request: operations.TeamsGetTeamByIdRequest,
    options?: RequestOptions,
  ): Promise<operations.TeamsGetTeamByIdResponse> {
    return unwrapAsync(teamsGetById(
      this,
      request,
      options,
    ));
  }

  /**
   * Update Team
   *
   * @remarks
   * Update organization team details.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async update(
    request: operations.TeamsUpdateTeamRequest,
    options?: RequestOptions,
  ): Promise<operations.TeamsUpdateTeamResponse> {
    return unwrapAsync(teamsUpdate(
      this,
      request,
      options,
    ));
  }

  /**
   * Remove Team
   *
   * @remarks
   * Remove team from the organization. Upon success, the team will be removed from the organization.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async remove(
    request: operations.TeamsRemoveTeamRequest,
    options?: RequestOptions,
  ): Promise<Uint8Array> {
    return unwrapAsync(teamsRemove(
      this,
      request,
      options,
    ));
  }

  /**
   * Get All Team Members
   *
   * @remarks
   * Returns all the team members of the organization.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `read` scope.
   */
  async listMembers(
    request: operations.TeamsGetAllTeamMembersRequest,
    options?: RequestOptions,
  ): Promise<operations.TeamsGetAllTeamMembersResponse> {
    return unwrapAsync(teamsListMembers(
      this,
      request,
      options,
    ));
  }

  /**
   * Get All Team Roles
   *
   * @remarks
   * Returns all the roles of the teamId mentioned in params.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `read` scope.
   */
  async getRoles(
    request: operations.TeamsGetAllTeamRolesRequest,
    options?: RequestOptions,
  ): Promise<operations.TeamsGetAllTeamRolesResponse> {
    return unwrapAsync(teamsGetRoles(
      this,
      request,
      options,
    ));
  }
}
