/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webformsCreate } from "../funcs/webformsCreate.js";
import { webformsGetAll } from "../funcs/webformsGetAll.js";
import { webformsGetById } from "../funcs/webformsGetById.js";
import { webformsRemove } from "../funcs/webformsRemove.js";
import { webformsUpdate } from "../funcs/webformsUpdate.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";

export class Webforms extends ClientSDK {
  /**
   * Get All Webforms
   *
   * @remarks
   * Returns all webforms of the given `owner_id` (teamId) in the request param.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `read` scope.
   */
  async getAll(
    request: operations.WebformsGetAllWebformsRequest,
    options?: RequestOptions,
  ): Promise<
    PageIterator<operations.WebformsGetAllWebformsResponse, { page: number }>
  > {
    return unwrapResultIterator(webformsGetAll(
      this,
      request,
      options,
    ));
  }

  /**
   * Create Webform
   *
   * @remarks
   * Add a webform to the organization. Returns the webform object in response.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async create(
    request: models.V3WebformsCreateOrUpdateWebformRequest,
    options?: RequestOptions,
  ): Promise<operations.WebformsCreateWebformResponse> {
    return unwrapAsync(webformsCreate(
      this,
      request,
      options,
    ));
  }

  /**
   * Update Webform
   *
   * @remarks
   * Update a webform to the organization. Returns the webform object in response.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async update(
    request: operations.WebformsUpdateWebformRequest,
    options?: RequestOptions,
  ): Promise<operations.WebformsUpdateWebformResponse> {
    return unwrapAsync(webformsUpdate(
      this,
      request,
      options,
    ));
  }

  /**
   * Remove Webform
   *
   * @remarks
   * Remove a webform from the organization.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `user-write` scope.
   */
  async remove(
    request: operations.WebformsRemoveWebformRequest,
    options?: RequestOptions,
  ): Promise<operations.WebformsRemoveWebformResponse> {
    return unwrapAsync(webformsRemove(
      this,
      request,
      options,
    ));
  }

  /**
   * Get Webform By ID
   *
   * @remarks
   * Returns a webform details of the given `webformId` in the request param.
   * Requires `access_token` as a `Bearer {{token}}` in the `Authorization` header with `read` scope.
   */
  async getById(
    request: operations.WebformsGetWebformByIdRequest,
    options?: RequestOptions,
  ): Promise<operations.WebformsGetWebformByIdResponse> {
    return unwrapAsync(webformsGetById(
      this,
      request,
      options,
    ));
  }
}
